'use strict'

let placesToVisit = ['New York', 'San Francisco', 'Miami', 'Las Vegas', 'Washington Dc'];

var i, len, text;
for (i = 0, len = placesToVisit.length, text = ""; i < len; i++) {
  text += placesToVisit[i] + "<br>";
}
